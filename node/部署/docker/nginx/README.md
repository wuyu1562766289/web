```js
// Dockerfile定制镜像
// FROM nginx:latest
// RUN echo '<h1>Hello, WangXing!</h1>' > /usr/share/nginx/html/index.html

// # 定制镜像
// docker build -t mynginx .
// # 运⾏行行
// # -d 守护态运⾏行行
// docker run -p 80:80 -d mynginx
```