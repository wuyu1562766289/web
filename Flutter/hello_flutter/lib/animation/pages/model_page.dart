import 'package:flutter/material.dart';

class WXModelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.orange,
      appBar: AppBar(
        title: Text("ModelPage"),
      ),
      body: Center(
        child: Text("ModelPageBody"),
      ),
    );
  }
}
